#pragma once

/**
 * A minimal, embeddable operating system written in C++11.
 */
namespace os
{
   struct q
   {
      template <typename T>
      static void en(T item)
      {
      }

      static void de()
      {
      }
   };
}
